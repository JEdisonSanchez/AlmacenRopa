@model IEnumerable<AlmacenRopa.Models.ROLE_USER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAME_ROLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION_ROLE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAME_ROLE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION_ROLE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ROLE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ROLE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ROLE })
        </td>
    </tr>
}

</table>
